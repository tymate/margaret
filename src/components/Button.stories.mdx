import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';
import Button from './Button';
import Stack from './Stack';
import { Buttons } from '../ui';

<Meta
  title="Design System/Button"
  component={Button}
  parameters={{
    previewTabs: { canvas: { hidden: true }, controls: { expanded: true } },
  }}
/>

# Button

<Canvas>
  <Story name="Button">
    <Stack gap={2} direction="column">
      <Stack gap={2}>
        <Button variant="primary">Hello Tymate</Button>
        <Button variant="outline">Hello Tymate</Button>
        <Button variant="ghost">Hello Tymate</Button>
      </Stack>
      <Stack gap={2}>
        <Button variant="primary" disabled>
          Hello Tymate
        </Button>
        <Button variant="outline" disabled>
          Hello Tymate
        </Button>
        <Button variant="ghost" disabled>
          Hello Tymate
        </Button>
      </Stack>
    </Stack>
  </Story>
</Canvas>
