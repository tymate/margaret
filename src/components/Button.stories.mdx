import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';
import Button from './Button';
import Stack from './Stack';
import { Buttons } from '../ui';

<Meta
  title="Design System/Button"
  component={Button}
  parameters={{
    previewTabs: { canvas: { hidden: true }, controls: { expanded: true } },
  }}
/>

# Button

<Canvas>
  <Story name="Button">
    <Button>Hello Tymate</Button>
  </Story>
</Canvas>

## Theming

The Button component accept a variant prop, which allows you to define stylistic variations to override the default values.
To add a variant to your button open the theme file (theme.js). It contains an object which holds values for common variables such as color, fonts, border, and more. In the button value you can add your variant with the style you want to apply.

Example with the variant primary:

```javascript
button: {
  primary: {
    background: 'var(--ui-primary)', // Can be hex, hsla, rgba…
    color: 'var(--colors-gray-0)',
    boxShadow: '',

    backgroundHover: 'var(--ui-primary-light)',
    colorHover: '',
    transformHover: '',
    boxShadowHover: '',

    backgroundDisabled: 'var(--ui-disabled)',
    colorDisabled: 'var(--colors-gray-400)',
    boxShadowDisabled: '',
    transformDisabled: '',

    paddingHorizontal: '24px',
    paddingVertical: '14px',
    borderRadius: '6px',
    minWidth: '220px',
  },
}
```

<Canvas>
  <Story name="Button with variant">
    <Stack direction="column" alignX="flex-start" gap={1}>
      <Button variant="primary">Hello Tymate</Button>
      <Button variant="primary" disabled>
        Hello Tymate
      </Button>
      <Button variant="primary" isLoading>
        Hello Tymate
      </Button>
      <Button
        variant="primary"
        isLoading
        loadingAnimation="line-spin-fade-loader"
      >
        Hello Tymate
      </Button>
    </Stack>
  </Story>
</Canvas>

<ArgsTable of={Button} />
