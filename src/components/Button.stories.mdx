import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';
import Button from './Button';
import { Buttons } from '../ui';

<Meta
  title="Design System/Button"
  component={Button}
  parameters={{
    previewTabs: { canvas: { hidden: true }, controls: { expanded: true } },
  }}
/>

export const Template = args => <Button {...args}>Hello Tymate</Button>;

# Button

<Canvas>
  <Story
    name="Button"
    argTypes={{
      disabled: {
        control: 'radio',
      },
      loader: {
        control: {
          type: 'select',
          options: [
            'ball-pulse',
            'ball-pulse-sync',
            'ball-scale',
            'ball-scale-random',
            'ball-rotate',
            'ball-clip-rotate',
            'ball-clip-rotate-pulse',
            'ball-clip-rotate-multiple',
            'ball-scale-ripple',
            'ball-scale-ripple-multiple',
            'ball-beat',
            'ball-scale-multiple',
            'ball-triangle-path',
            'ball-pulse-rise',
            'ball-grid-beat',
            'ball-grid-pulse',
            'ball-spin-fade-loader',
            'ball-spin-loader',
            'ball-zig-zag',
            'ball-zig-zag-deflect',
            'line-scale',
            'line-scale-random',
            'line-scale-pulse-out',
            'line-scale-pulse-out-rapid',
            'line-spin-fade-loader',
            'triangle-skew-spin',
            'square-spin',
            'pacman',
            'cube-transition',
            'semi-circle-spin',
          ],
        },
      },
    }}
    args={{ loader: 'ball-rotate' }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Button} />
