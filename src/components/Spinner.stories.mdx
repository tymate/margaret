import { Meta, Story, Preview, Props } from '@storybook/addon-docs';
import { boolean, select } from '@storybook/addon-knobs';
import { action } from '@storybook/addon-actions';
import Spinner from './Spinner';
import Button from './Button';
import { Stack } from '../ui';

<Meta
  title="Design System/Spinner"
  component={Spinner}
  parameters={{ previewTabs: { canvas: { hidden: true } } }}
/>

# Spinner

<Props of={Spinner} />

## Default

<Preview>
  <Story name="DefaultSpinner">
    <Stack direction="column" gutterSize={0.5} size="full" alignX="center">
      <Spinner />
      <Spinner />
      <Spinner />
      <Spinner />
      <Spinner />
      <Spinner />
      <Spinner />
      <Spinner />
    </Stack>
  </Story>
</Preview>

## Buttons

<Preview>
  <Story name="Buttons">
    <Stack direction="column" gutterSize={0.5}>
      <Button
        isLoading
        variant="primary"
        loadingAnimation="line-spin-fade-loader"
      >
        Hello Tymate
      </Button>
      <Button isLoading variant="primary">
        Hello Tymate
      </Button>
      <Button isLoading variant="primary" loadingAnimation="ball-pulse">
        Hello Tymate
      </Button>
    </Stack>
  </Story>
</Preview>
