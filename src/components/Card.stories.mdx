import { Meta, Story, Preview } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';
import { Card } from './';
import { MyCardFooter } from '../sandbox';

<Meta
  title="Design System/Card"
  component={Card}
  parameters={{ previewTabs: { canvas: { hidden: true } } }}
/>

# Card

## Default
